<p id="notice"><%= notice %></p>
<li>
    <%= link_to user.email, user %>
  <% if current_user.admin? && !current_user?(user) %>
    <a id="delete_user" class="a_product" href="#" user=<%=user.id%>>Delete</a>
    
  <% end %>
</li>